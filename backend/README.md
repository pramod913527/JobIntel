# Backend

This folder contains a minimal Express + TypeScript starter for JobScout's backend.

Quick start:

```bash
cd backend
npm install
npm run dev
```

The server exposes a health endpoint at `/api/health`.
