# Database

This folder is reserved for database-related code (migrations, seeds, connection helpers).

Suggested starter:

- `database/sqlite/` - SQLite files and migrations
- `database/knexfile.ts` or an ORM configuration

Add migrations and seeds here when ready.
